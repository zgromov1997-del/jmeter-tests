# JMeter Tests

Repository for JMeter scripts and Jenkins automation.

## Structure

- test-plans/ — JMeter test plans
- data/ — test data (CSV, payloads)
- properties/ — configuration files
- scripts/ — helper scripts
- results/ — test results (ignored in git)